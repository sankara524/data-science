CREATE DATABASE ER;

USE ER;

-- DEPARTMENT TABLE
CREATE TABLE DEPT
(
D_ID INT PRIMARY KEY,
D_NAME VARCHAR(20)
);

SHOW TABLES;

-- EMPLOYEE TABLE
CREATE TABLE EMP
(
E_ID INT PRIMARY KEY,
E_NAME VARCHAR(30),
DEPT_ID INT,
FOREIGN KEY (DEPT_ID) REFERENCES DEPT(D_ID)
);

SHOW TABLES;