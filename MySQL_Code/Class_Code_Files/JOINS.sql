USE ER;

SHOW TABLES;

SELECT * FROM DEPT;
SELECT * FROM EMP;

-- FROM DEPARTMENT TABLE
INSERT INTO DEPT VALUES
(10, 'IT'),
(20, 'FINANCE'),
(30, 'HR');

SELECT * FROM DEPT;

-- FROM EMPLOYEE TABLE
INSERT INTO EMP VALUES
(1, 'PRIYA', 10),
(2, 'VARSHA', 10),
(3, 'SNEHA', 30),
(4, 'VINEELA', NULL);

SELECT * FROM EMP;


-- 1. INNER JOIN
SELECT E.E_NAME, D.D_NAME
FROM EMP E
INNER JOIN DEPT D
ON E.DEPT_ID = D.D_ID;

-- OR
SELECT E.E_ID, E.E_NAME, E.DEPT_ID, D.D_NAME
FROM EMP E
INNER JOIN DEPT D
ON E.DEPT_ID = D.D_ID;

-- OR 
SELECT *
FROM EMP E
INNER JOIN DEPT D
ON E.DEPT_ID = D.D_ID;

-- 2. LEFT JOIN
SELECT *
FROM EMP E
LEFT JOIN DEPT D
ON E.DEPT_ID = D.D_ID;

-- 3. RIGHT JOIN
SELECT *
FROM EMP E
RIGHT JOIN DEPT D
ON E.DEPT_ID = D.D_ID;

-- CROSS JOIN
SELECT * 
FROM EMP E
CROSS JOIN DEPT D;


-- SELF JOIN
CREATE TABLE STAFF
(
ID INT,
NAME VARCHAR(30),
M_ID INT
);

INSERT INTO STAFF VALUES
(1, 'PRIYA', 3),
(2, 'VARSHA', 3),
(3, 'SNEHA', NULL),
(4, 'VINEELA', 3);

SELECT * FROM STAFF;

-- SELF JOIN
SELECT S1.NAME, S2.NAME
FROM STAFF S1
JOIN STAFF S2
ON S1.M_ID = S2.ID;